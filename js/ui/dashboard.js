/**
 * OBSOLETO — el Dashboard y la vista de Equipos se unificaron en `panel.js`.
 * Se mantiene solo como re-export para no romper imports viejos.
 */
export { renderPanel as renderDashboard } from './panel.js';
