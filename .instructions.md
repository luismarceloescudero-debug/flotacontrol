# ROO CODE: FLOTACONTROL - INSTRUCCIONES EJECUTABLES

**OBJETIVO:** Reparar FlotaControl aplicando 5 cambios críticos

---

## 🚨 PROBLEMA CENTRAL

App NO FUNCIONA porque **no normaliza datos entre archivos Excel**

**Ejemplo:**
- Equipos.xlsx: `interno = "TR21"`
- Cargas.xlsx: `interno = "TR 21"` (con espacio)
- GPS.xlsx: `unidad = "TR-21"` (con guión)

**Resultado:** La app ve 3 equipos DIFERENTES cuando es el MISMO
→ No cruza datos → Consumos incorrectos → TODO FALLA

---

## ✅ SOLUCIÓN: 5 CAMBIOS EN 4 ARCHIVOS

### CAMBIO 1: `js/data/normalizer.js`
**Acción:** Agregar funciones de normalización

**Ubicación:** Antes de `export function extractDataFromString(val)`

**Código a agregar:**
```javascript
export function normalizeEquipoKey(interno) {
    if (!interno) return '';
    return normalizeString(interno).replace(/[\s\-_]/g, '').trim();
}
```

**Luego: Modificar `extractDataFromString()` para retornar `interno_key`**

**Ubicación:** En el return de la función

**Cambiar de:**
```javascript
return { interno, dominio };
```

**A:**
```javascript
return { 
    interno, 
    dominio, 
    interno_key: normalizeEquipoKey(interno)
};
```

**Finalmente: Agregar al FINAL del archivo**
```javascript
export function parseDuration(val) {
    if (!val) return 0;
    if (typeof val === 'number') return val;
    let str = String(val).trim();
    if (str.includes(':')) {
        let parts = str.split(':');
        if (parts.length === 3) {
            let h = parseNumber(parts[0]) || 0;
            let m = parseNumber(parts[1]) || 0;
            let s = parseNumber(parts[2]) || 0;
            return h + (m / 60) + (s / 3600);
        }
    }
    return parseNumber(str);
}

export function aggregateHours(horasDict) {
    if (!horasDict || typeof horasDict !== 'object') {
        return { ralenti: 0, movimiento: 0, parado: 0, total: 0 };
    }
    const hr = parseDuration(horasDict.ralenti || 0) || 0;
    const hm = parseDuration(horasDict.movimiento || 0) || 0;
    const hp = parseDuration(horasDict.parado || 0) || 0;
    const total = hr + hm + hp;
    return {
        ralenti: Math.round(hr * 100) / 100,
        movimiento: Math.round(hm * 100) / 100,
        parado: Math.round(hp * 100) / 100,
        total: Math.round(total * 100) / 100
    };
}
```

---

### CAMBIO 2: `js/data/database.js`
**Acción:** Agregar función `getAllRawRecords()`

**Ubicación:** DESPUÉS de la función `getAllEstimados()`

**Código a agregar:**
```javascript

export function getAllRawRecords() {
    return new Promise((resolve, reject) => {
        const transaction = getDB().transaction(['raw_records'], 'readonly');
        const store = transaction.objectStore('raw_records');
        const request = store.getAll();
        request.onsuccess = () => resolve(request.result);
        request.onerror = (e) => reject(e.target.error);
    });
}
```

---

### CAMBIO 3: `js/data/analyzer.js`
**Acción:** 3 modificaciones

**3A: Agregar import al inicio**

**Ubicación:** Después del comentario de bloque (///), al inicio

**Código:**
```javascript
import { normalizeEquipoKey } from './normalizer.js';
```

**3B: Mejorar `calculateMetrics()` - Sección de horas**

**Buscar:**
```javascript
gpsList.forEach(g => {
    totalKm += (parseFloat(g.distancia) || 0);
    totalHoras += (parseFloat(g.horas) || 0);
});
```

**Cambiar a:**
```javascript
gpsList.forEach(g => {
    totalKm += (parseFloat(g.distancia) || 0);
    if (typeof g.horas === 'object' && g.horas.total) {
        totalHoras += g.horas.total;
    } else {
        totalHoras += (parseFloat(g.horas) || 0);
    }
});
```

**3C: Agregar funciones de cruce al FINAL**

**Ubicación:** Antes del último `}` del archivo

**Código:**
```javascript

export function getCargasForEquipo(interno, rawRecords) {
    return rawRecords.filter(r => 
        r.type === 'carga' && 
        normalizeEquipoKey(r.interno) === normalizeEquipoKey(interno)
    );
}

export function getGPSForEquipo(interno, rawRecords) {
    return rawRecords.filter(r => 
        r.type === 'gps' && 
        normalizeEquipoKey(r.interno) === normalizeEquipoKey(interno)
    );
}
```

---

### CAMBIO 4: `js/export/exporter.js`
**Acción:** Implementar `exportTableToXLSX()` funcional

**Ubicación:** Reemplazar toda la función `exportTableToXLSX()`

**Cambiar de:**
```javascript
export function exportTableToXLSX() {
    alert(`Exportando tabla a Excel...\n(La generación real se implementará usando SheetJS)`);
    // TODO: Implement actual SheetJS write logic here
}
```

**A:**
```javascript
export function exportTableToXLSX() {
    const table = document.getElementById('data-table');
    if (!table) {
        alert('No hay tabla para exportar');
        return;
    }
    try {
        const ws = XLSX.utils.table_to_sheet(table);
        const wb = XLSX.utils.book_new();
        XLSX.utils.book_append_sheet(wb, ws, 'Datos');
        const filename = `FlotaControl_${new Date().toISOString().split('T')[0]}.xlsx`;
        XLSX.writeFile(wb, filename);
        console.log('✅ Exportado:', filename);
    } catch (e) {
        console.error('Error:', e);
        alert('Error: ' + e.message);
    }
}
```

---

## 🧪 VALIDACIÓN

Después de aplicar todos los cambios, ejecutar en consola (F12):

```javascript
console.log(typeof normalizeEquipoKey === 'function' ? "✅ normalizeEquipoKey" : "❌");
console.log(typeof aggregateHours === 'function' ? "✅ aggregateHours" : "❌");
console.log(typeof parseDuration === 'function' ? "✅ parseDuration" : "❌");
getAllRawRecords().then(r => console.log(`✅ getAllRawRecords: ${r.length}`)).catch(e => console.log("❌", e.message));
getAllEstimados().then(r => console.log(`✅ getAllEstimados: ${r.length}`)).catch(e => console.log("❌", e.message));
```

**Si ves 5 ✅, está LISTO**

---

## 🎯 PRÓXIMO PASO

1. Sube los 4 archivos Excel (Equipos, Cargas, GPS, Estimados)
2. Presiona "🚀 Procesar y Analizar"
3. Revisa Dashboard - debe mostrar números correctos
