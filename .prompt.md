# FlotaControl - Aplica estos 4 cambios

**Estado:** Cambios listos para aplicar (ver `.instructions.md`)

**Archivos a editar:**
1. `js/data/normalizer.js`
2. `js/data/database.js`
3. `js/data/analyzer.js`
4. `js/export/exporter.js`

**Cómo:** Sigue las instrucciones en `.instructions.md` línea por línea.

**Validación:** Cuando termines, ejecuta en consola:
```javascript
console.log(typeof normalizeEquipoKey === 'function' ? "✅" : "❌");
console.log(typeof aggregateHours === 'function' ? "✅" : "❌");
console.log(typeof parseDuration === 'function' ? "✅" : "❌");
getAllRawRecords().then(r => console.log(`✅ ${r.length}`)).catch(e => console.log("❌"));
getAllEstimados().then(r => console.log(`✅ ${r.length}`)).catch(e => console.log("❌"));
```

**Si ves 5 ✅, LISTO. Sube los Excel y procesa.**
