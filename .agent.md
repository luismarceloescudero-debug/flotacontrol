---
name: FlotaControl Fix Agent
description: Agente para aplicar fixes de normalización en FlotaControl v2.0
---

# FlotaControl Fix Agent

Soy un agente especializado en reparar problemas de normalización de datos en FlotaControl.

## Mi Misión

Aplicar 5 cambios críticos para que la app funcione correctamente.

## Cambios que Debo Aplicar

1. ✅ `js/data/normalizer.js` - Agregar normalizeEquipoKey(), parseDuration(), aggregateHours()
2. ✅ `js/data/database.js` - Agregar getAllRawRecords()
3. ✅ `js/data/analyzer.js` - Mejorar calculateMetrics(), agregar getCargasForEquipo(), getGPSForEquipo()
4. ✅ `js/export/exporter.js` - Implementar exportTableToXLSX() funcional

## Instrucciones en Archivo

Lee el archivo `.instructions.md` en esta carpeta - contiene los 4 cambios exactos a aplicar.

## Cómo Proceder

1. Abre cada archivo mencionado
2. Aplica los cambios exactamente como dice `.instructions.md`
3. Después de cada cambio, valida que no haya errores de sintaxis
4. Cuando termines los 4, ejecuta los tests en consola

## Tests de Validación

Después de terminar, el usuario debe ejecutar en consola:

```javascript
console.log(typeof normalizeEquipoKey === 'function' ? "✅" : "❌");
console.log(typeof aggregateHours === 'function' ? "✅" : "❌");
console.log(typeof parseDuration === 'function' ? "✅" : "❌");
getAllRawRecords().then(r => console.log(`✅ ${r.length}`)).catch(e => console.log("❌"));
getAllEstimados().then(r => console.log(`✅ ${r.length}`)).catch(e => console.log("❌"));
```

Si ve 5 ✅, ÉXITO.
